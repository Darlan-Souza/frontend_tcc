<h3>Apresentações marcadas</h3>
<table class="table">
    <thead class="thead-dark">
        <tr>
            <th scope="col">Título</th>
            <th scope="col">Aluno</th>
            <th scope="col">Data</th>
            <th scope="col">Hora</th>
            <th scope="col">Detalhe</th>
        </tr>
    </thead>
    <tbody>
        {{#each trabalhos}}
        <tr>
            <th scope="row">{{titulo}}</th>
            <td>{{orientando}}</td>
            <td>{{data}}</td>
            <td align="left">{{horario}}</td>
            <td align="left"><a href="/tcc/cadastro/exibe/{{_id}}"><button class="btn btn-primary px-3 py-1"><i
                            class="fas fa-bars" style="color: white"></i></a></td>
        </tr>
        {{else}}
        {{/each}}
    </tbody>
</table>