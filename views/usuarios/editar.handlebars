{{#each erros}}

<div class="alert alert-danger">{{texto}}</div>

{{else}}

{{/each}}
<h3>Editar aluno</h3>

<div class="card" style="margin-bottom: 10em;">
    <div class="card-body">
        <h5>Pesuise o aluno</h5>
        <form class="form-inline">
            <input class="form-control mr-2 col-sm-5" type="search" placeholder="Pesquisar aluno" aria-label="Search">
            <button class="btn btn-outline-success my-2 my-sm-0 col-sm-2" type="submit">Pesquisar</button>
        </form>

        <br>
        <h5>Dados do aluno</h5>
        <form action="" method="POST">
            <label for="nome">Nome: </label>
            <input type="text" name="nome" class="form-control" required>
            <label for="email">E-mail: </label>
            <input type="email" name="email" class="form-control" required>
           
            <button type="submit" class="btn btn-success mt-3">Salvar alterações</button>
        </form>
    </div>
</div>